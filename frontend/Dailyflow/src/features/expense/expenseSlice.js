import  {createSlice} from '@reduxjs/toolkit'

const initialState = {}

export const expenseSlice = createSlice({
  name:'counter',
  initialState,
  reducers:{
    getExpense: (state)=>{
      
    }
  }



})